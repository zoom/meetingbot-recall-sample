import React from 'react';
import './Fallback.css';

function Fallback() {
    return (
        <div className="Fallback">
            <p>Start a meeting to generate your AI meeting notes.</p>
        </div>
    );
}

export default Fallback;
